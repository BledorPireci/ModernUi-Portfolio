{
  "name": "portfolio2",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:css && npm run build:js",
    "preview": "vite preview",
    "build:css": "sass style.scss style.min.css --style=compressed",
    "build:js": "terser main.js -o main.min.js --compress --mangle",
    "watch:css": "sass style.scss style.min.css --watch --style=compressed"
  },
  "devDependencies": {
    "csso": "^5.0.5",
    "sass": "^1.85.1",
    "terser": "^5.39.0",
    "vite": "^5.0.8"
  }
}
